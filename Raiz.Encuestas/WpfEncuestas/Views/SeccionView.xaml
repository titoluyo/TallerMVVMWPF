<UserControl x:Class="WpfEncuestas.Views.SeccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="150" Width="451">
    <Grid>
        <Label Content=" Ingrese Seccion :" HorizontalAlignment="Left" Margin="32,45,0,0" VerticalAlignment="Top"/>
        <TextBox Text="{Binding Nombre}" HorizontalAlignment="Left" Height="23" Margin="163,48,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="168"/>
        <Button x:Name="BtnGrabar" Visibility="Visible" Content="Agregar" Command="{Binding GrabarCommand}" HorizontalAlignment="Left" Margin="353,47,0,0" VerticalAlignment="Top" Width="75"/>
        <Button x:Name="BtnCancelar" Visibility="Visible" Content="Cancelar" Command="{Binding CancelarCommand}" HorizontalAlignment="Left" Margin="353,74,0,0" VerticalAlignment="Top" Width="75"/>
        <Button x:Name="BtnBuscar" Content="Buscar Sección" Command="{Binding BuscarCommand}" HorizontalAlignment="Left" Margin="353,101,0,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</UserControl>
