<UserControl x:Class="WpfEncuestas.Views.FlujoListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="500">
    <Grid>
        <ListView HorizontalAlignment="Left" Height="115" Margin="34,61,0,0" VerticalAlignment="Top" Width="419">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Evaluador" DisplayMemberBinding="{Binding Opcion}" Width="100"/>
                    <GridViewColumn Header="Evaluado" DisplayMemberBinding="{Binding Ponderacion}" Width="100"/>
                    <GridViewColumn Header="Plantilla" DisplayMemberBinding="{Binding Ponderacion}" Width="100"/>
                    <GridViewColumn Header="Zona" DisplayMemberBinding="{Binding Ponderacion}" Width="100"/>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button x:Name="BtnAgregarFlujo"  Content="Agregar Nuevo" HorizontalAlignment="Left" Margin="34,21,0,0" VerticalAlignment="Top" Width="95"/>

    </Grid>
</UserControl>
