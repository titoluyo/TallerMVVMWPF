<UserControl x:Class="WpfEncuestas.Views.SeccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="100" Width="420">
    <Grid>
        <Label Content=" Ingrese Seccion :" HorizontalAlignment="Left" Margin="10,9,0,0" VerticalAlignment="Top"/>
        <TextBox Text="{Binding Nombre}" HorizontalAlignment="Left" Height="23" Margin="141,12,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="168"/>
        <Button x:Name="BtnGrabar" Visibility="Visible" Content="Agregar" Command="{Binding GrabarCommand}" HorizontalAlignment="Left" Margin="331,11,0,0" VerticalAlignment="Top" Width="75"/>
        <Button x:Name="BtnCancelar" Visibility="Visible" Content="Cancelar" Command="{Binding CancelarCommand}" HorizontalAlignment="Left" Margin="331,38,0,0" VerticalAlignment="Top" Width="75"/>
        <Button x:Name="BtnBuscar" Content="Buscar Sección" Command="{Binding BuscarCommand}" HorizontalAlignment="Left" Margin="331,65,0,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</UserControl>
